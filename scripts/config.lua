

local config = {

    io_buffer_size = 4,         -- count of slot in local io buffer
    default_delivery = 100,
    spill_items_on_ground = false,
    place_items_in_inventory = false


}

return config